node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Uploaded files (local file storage) — keep the folder, ignore its contents.
apps/web/public/uploads/*
!apps/web/public/uploads/.gitkeep
