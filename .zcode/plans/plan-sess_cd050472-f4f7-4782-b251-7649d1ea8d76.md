Реализуем метод `create` — создание товара с образцами (exemplars) атомарно — в контракте `IProductsRepository` и в `DrizzleProductsRepository`. Зафиксировано: загрузка файлов уходит в use case (application), репозиторий остаётся URL-based и «глупым».

## Изменения (2 файла)

### 1. `packages/domain/src/ports/products-repository.port.ts`
- Добавить `import type { PriceInRub }` (из shared) и `Size` (из ../products).
- Добавить два DTO рядом с `UpdateProductData`:
  - `CreateExemplarData { price: PriceInRub; inStock: boolean; size: Size }`
  - `CreateProductData { name: string; description: string; category: UniqueId; images: ImageUrl[]; exemplars: CreateExemplarData[] }`
- В `IProductsRepository` первым методом: `create(data: CreateProductData): Promise<IProduct>`.

### 2. `packages/infrastructure/src/repositories/drizzle-products.repository.ts`
Реализовать `create` через Drizzle-транзакцию:
- insert product → `.returning({ id })` → insert exemplars (если есть) → вернуть id из транзакции
- `return (await this.getById(id))!` — переиспользует существующий getById (JOIN + groupProducts)
- camelCase→snake_case маппинг (category→categoryId)
- добавить `CreateProductData` в существующий `import type {...} from '@panda-lavanda/domain'`

## Не делается
- Не чиню pre-existing баг `ProductWithExemplars` (строка 98)
- Не заворачиваю в `Either`
- Не добавляю use case / маршрут / UI / тесты

## Проверка
- `npx tsc --noEmit` в domain/infrastructure/web (pre-existing баг `ProductWithExemplars` останется, новые ошибки не добавляю)